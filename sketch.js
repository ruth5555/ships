
function preload(){
seaImg=loadImage("sea.png");
shipImg1=loadAnimation(//list the names of png files that you want to load);
}

function setup(){
  createCanvas(400,400);
 spriteName.
  Dreamstime.com242 Alone Boat Middle Sea Photos - Free & Royalty-Free Stock Photos from Dreamstime(seaImg);
}

function draw() {
  background("blue");
 //code to reset the background
 if(sea.x<0){
   sea.x=sea.width/2
 }
}